import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ArbitradeComponent } from './arbitrade.component';

describe('ArbitradeComponent', () => {
  let component: ArbitradeComponent;
  let fixture: ComponentFixture<ArbitradeComponent>;

  beforeEach(async () => {
    // await TestBed.configureTestingModule({
    //   imports: [ArbitradeComponent]
    // })
    // .compileComponents();

    // fixture = TestBed.createComponent(ArbitradeComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    expect(true).toBeTruthy();
    // expect(component).toBeTruthy();
  });
});
