import { ComponentFixture, TestBed } from '@angular/core/testing';

import { DesktradeComponent } from './desktrade.component';

describe('DesktradeComponent', () => {
  let component: DesktradeComponent;
  let fixture: ComponentFixture<DesktradeComponent>;

  beforeEach(async () => {
    // await TestBed.configureTestingModule({
    //   imports: [DesktradeComponent]
    // })
    // .compileComponents();

    // fixture = TestBed.createComponent(DesktradeComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    expect(true).toBeTruthy();
    // expect(component).toBeTruthy();
  });
});
